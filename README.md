# CLI TETRIS with C++

TETRIS for your boring classes.

## How to Build and Run

Clone this reporsitory.
```bash
git clone https://github.com/kmwook03/tetris_cpp.git
```

Build with CMakeLists
```bash
mkdir -p build && cd build && cmake .. && make
```

Run `app`
```bash
./build/bin/app
```
